################################################################################
# GAIA RESERVATION - Configuration d'environnement
# 
# INSTRUCTIONS :
# 1. Copiez ce fichier vers .env : cp .env.example .env
# 2. Remplacez TOUTES les valeurs par vos vraies configurations
# 3. Ne commitez JAMAIS le fichier .env dans Git !
#
# ⚠️  ATTENTION : Les valeurs ci-dessous sont des EXEMPLES à des fins 
#    pédagogiques. Utilisez des clés sécurisées en production !
################################################################################

#==============================================================================
# BASE DE DONNÉES POSTGRESQL
#==============================================================================

# Chaîne de connexion complète à PostgreSQL
# Format: Host=host;Port=port;Database=db;Username=user;Password=pwd;Include Error Detail=true
DB_CONNECTION_STRING=Host=localhost;Port=5432;Database=gaiacabinet_dev;Username=dev_user;Password=CHANGE_ME_STRONG_PASSWORD;Include Error Detail=true

# Séparées pour plus de flexibilité (optionnel)
# DB_HOST=localhost
# DB_PORT=5432
# DB_NAME=gaiacabinet_dev
# DB_USER=dev_user
# DB_PASSWORD=CHANGE_ME_STRONG_PASSWORD

#==============================================================================
# AUTHENTIFICATION JWT
#==============================================================================

# Émetteur du token (généralement l'URL de votre API)
AUTHJWT__ISSUER=http://localhost:5111

# Audience du token (généralement l'URL de votre application frontend)
AUTHJWT__AUDIENCE=http://localhost:3000

# Clé de signature JWT (MINIMUM 32 caractères, utilisez une clé forte !)
# Générez une clé sécurisée : openssl rand -base64 32
AUTHJWT__SIGNINGKEY=CHANGE_ME_TO_A_VERY_LONG_AND_RANDOM_KEY_MIN_32_CHARS_FOR_SECURITY

# Durée de vie du token d'accès (en minutes)
# Recommandé : 15-60 minutes pour la sécurité
AUTHJWT__ACCESSTOKENMINUTES=15

# Durée de vie du token de rafraîchissement (en jours)
# Recommandé : 7-30 jours selon votre politique de sécurité
AUTHJWT__REFRESHTOKENDAYS=30

#==============================================================================
# OTP (ONE-TIME PASSWORD) / 2FA
#==============================================================================

# Clé pepper pour sécuriser les codes OTP (MINIMUM 32 caractères)
# Cette clé doit rester secrète et être unique par environnement
# Générez une clé sécurisée : openssl rand -base64 32
OTPPEPPER__OTPPEPPER=CHANGE_ME_TO_ANOTHER_VERY_LONG_AND_RANDOM_KEY_MIN_32_CHARS

#==============================================================================
# ENVIRONNEMENT & LOGGING
#==============================================================================

# Environnement de déploiement (Development, Staging, Production)
ASPNETCORE_ENVIRONMENT=Development

# Niveau de logging (Trace, Debug, Information, Warning, Error, Critical)
LOGGING__LOGLEVEL__DEFAULT=Information
LOGGING__LOGLEVEL__MICROSOFT=Warning

#==============================================================================
# SERVEUR & RÉSEAU
#==============================================================================

# URLs d'écoute du serveur
# Exemple : http://localhost:5111;https://localhost:5112
ASPNETCORE_URLS=http://localhost:5111

# Port HTTP (optionnel si défini dans URLS)
# HTTP_PORT=5111

# Port HTTPS (optionnel si défini dans URLS)  
# HTTPS_PORT=5112

#==============================================================================
# CORS (Cross-Origin Resource Sharing)
#==============================================================================

# Origines autorisées pour CORS (séparées par des virgules)
# En développement : localhost. En production : vos domaines réels
CORS__ALLOWEDORIGINS=http://localhost:3000,http://localhost:3001,https://localhost:3000

#==============================================================================
# EMAIL / NOTIFICATIONS (Si implémenté)
#==============================================================================

# Configuration SMTP pour l'envoi d'emails
# EMAIL__SMTP__HOST=smtp.gmail.com
# EMAIL__SMTP__PORT=587
# EMAIL__SMTP__USERNAME=your-email@gmail.com
# EMAIL__SMTP__PASSWORD=your-app-password
# EMAIL__SMTP__ENABLESSL=true

# Expéditeur par défaut
# EMAIL__FROM__NAME=GAIA Cabinet
# EMAIL__FROM__ADDRESS=noreply@gaiacabinet.com

#==============================================================================
# STORAGE / FICHIERS (Si implémenté)
#==============================================================================

# Dossier de stockage local des fichiers uploadés
# STORAGE__LOCAL__PATH=./uploads

# Taille maximale des fichiers (en MB)
# STORAGE__MAXFILESIZE=10

# Extensions autorisées (séparées par des virgules)
# STORAGE__ALLOWEDEXTENSIONS=.jpg,.jpeg,.png,.pdf,.doc,.docx

#==============================================================================
# CACHE / REDIS (Si implémenté)
#==============================================================================

# Configuration Redis pour le cache distribué
# REDIS__CONNECTIONSTRING=localhost:6379
# REDIS__INSTANCENAME=GaiaCabinet

#==============================================================================
# MONITORING & HEALTH CHECKS
#==============================================================================

# URL de vérification de santé de l'application
# HEALTHCHECKS__UI__WEBHOOKS=http://localhost:5111/health

#==============================================================================
# SÉCURITÉ ADDITIONNELLE
#==============================================================================

# Clé pour le chiffrement des données sensibles (si implémenté)
# DATA_PROTECTION_KEY=ANOTHER_VERY_SECURE_KEY_FOR_DATA_PROTECTION

# Timeout des sessions (en minutes)
# SESSION_TIMEOUT=30

# Nombre maximum de tentatives de connexion échouées
# MAX_LOGIN_ATTEMPTS=5

# Durée de blocage après échecs (en minutes)
# LOCKOUT_DURATION=15

################################################################################
# NOTES IMPORTANTES :
#
# 🔐 SÉCURITÉ :
# - Changez TOUTES les clés et mots de passe par défaut
# - Utilisez des clés de 32+ caractères pour JWT et OTP
# - Ne partagez JAMAIS vos vraies variables d'environnement
#
# 🏗️ ENVIRONNEMENTS :
# - Utilisez des valeurs différentes pour dev/staging/prod
# - Les URLs et ports doivent correspondre à votre infrastructure
#
# 📝 MAINTENANCE :
# - Documentez tout changement dans ce fichier
# - Informez l'équipe des nouvelles variables requises
#
# 🎯 GÉNÉRATION DE CLÉS SÉCURISÉES :
# - openssl rand -base64 32  (pour les clés de 32+ chars)
# - uuidgen  (pour des identifiants uniques)
################################################################################
